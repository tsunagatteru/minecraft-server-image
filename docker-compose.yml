version: '2'
services:
  minecraft:
    build: .
    ports:
      - "25565:25565"
    environment:
      RAM_LIMIT: '2G'
      AUTH_SERVER: 'ely.by'
      EULA: 'true'
    volumes:
      - minecraft-data:/data

volumes:
  minecraft-data:
